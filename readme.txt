>to run you need to install oracle11g XE and set JAR files to the JRE library

>change the database connection url and password,same as oracle setup ID,Password.

>run create table only first time to create table in your database

>create schema 'ROHIT' in your database

			OR

 change all query in all the java files with your schema name i.e change with your schema name where rohit is written


>run homepage to start the management system